# The code works

hrs = input('Enter Hours:')
rate = input('Enter Rate:')
ah = float(hrs)
ar = float(rate)

if ah > 40 :
    aba = ah * ar
    abb = (ah - 40.0) * (ar * 0.5)
    abc = aba + abb
    
else :
    abc = ah * ar 
    
print(abc)
